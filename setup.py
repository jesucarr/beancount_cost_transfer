from setuptools import setup

setup(
    name='beancount-cost-transfer',
    version='1.0',
    description='Beancount plugin to transfer cost basis to a different account',
    packages=['beancount_cost_transfer'],
    license='MIT',
)